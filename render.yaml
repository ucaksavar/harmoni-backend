services:
  - type: web
    name: harmoni-backend
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      curl -fsSL https://deno.land/install.sh | sh
      echo 'export PATH="$HOME/.deno/bin:$PATH"' >> ~/.bashrc
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT
